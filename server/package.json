{
  "name": "daily-tasks-push-server",
  "version": "1.0.0",
  "private": true,
  "description": "Minimal Node.js server that stores per-device reminder schedules in JSON files and sends push notifications via Firebase Cloud Messaging when they're due.",
  "type": "commonjs",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "firebase-admin": "^12.3.1",
    "node-cron": "^3.0.3"
  }
}
